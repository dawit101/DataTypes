# <- the sign means to make a 'comment'

#Fundamental Data Types

int #integer means numbers
float #Convert a string or number to a floating point number, if possible.
bool #Returns True when the argument x is true, False otherwise
str #Create a new string object from the given object
list #If no argument is given, the constructor creates a new empty list. The argument must be an iterable if specified.
tuple #If no argument is given, the constructor returns an empty tuple. If iterable is specified the tuple is initialized from iterable's items. If the argument is a tuple, the return value is the same object.
set #Build an unordered collection of unique elements.
dict #

#Classes = means custom data types

#Specialized Data Types = special modules that can be added and used

#None = nothing
